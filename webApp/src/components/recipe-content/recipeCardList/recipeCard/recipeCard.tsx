
import React from 'react';
import { useStyles } from './recipeCard.styles';

interface IRecipeCardProps {

}

const RecipeCard = ({ }: IRecipeCardProps) => {
  const classes = useStyles();

  return (
    <>
    </>
  );
};

export default RecipeCard;
