import * as React from "react";
import RecipesContainer from "../../components/recipe-content/recipesContainer"

interface IRecipeProps {}

const Recipe = () => {
  return (
    <div>
      <RecipesContainer />
    </div>
  );
};

export default Recipe;
